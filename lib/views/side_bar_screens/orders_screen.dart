import 'package:flutter/cupertino.dart';

class OrdersScreen extends StatelessWidget {
  static const String id = '\order-screen';
  const OrdersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('صفحه سفارشات'),
    );
  }
}