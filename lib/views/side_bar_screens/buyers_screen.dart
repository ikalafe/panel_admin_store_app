import 'package:flutter/cupertino.dart';

class BuyersScreen extends StatelessWidget {
  static const String id = '\buyer-screen';
  const BuyersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('صفحه خریداران'),
    );
  }
}
